# Public-Safe Active Configuration

The configuration in this directory is a portable template for the selected
audit-fixed recipe. It intentionally uses placeholders because the private dataset
and starting checkpoint are not part of this repository mirror. Substitute paths in
your own environment only after reproducing the documented dataset lineage.
